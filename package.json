{
  "name": "@exasol/extension-parameter-validator",
  "version": "0.2.0",
  "description": "Validator for Exasol extension parameters",
  "license": "MIT",
  "main": "dist/extensionParameterValidator.js",
  "types": "dist/extensionParameterValidator.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "jest"
  },
  "dependencies": {
    "@exasol/extension-manager-interface": "0.1.8"
  },
  "devDependencies": {
    "typescript": "4.7.4",
    "ts-node": "^10.9.1",
    "@types/jest": "^28.1.6",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.7"
  }
}
